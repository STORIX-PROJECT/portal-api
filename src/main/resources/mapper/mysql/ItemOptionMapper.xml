<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.storix.portalapi.mapper.item.ItemOptionMapper">


    <select id="getValidOptions" resultType="java.lang.Long">
        SELECT  OPTION_NO
          FROM  ITEM_OPTION
         WHERE  GROUP_NO IN (
                SELECT  GROUP_NO
                  FROM  ITEM_OPTION_GROUP
                 WHERE  ITEM_NO = #{itemNo}
        )
    </select>

    <!--출력 쿼리문-->
    <select id="getOptionDetails" resultType="com.shop.storix.portalapi.model.dto.item.option.ItemOptionDto$OptionDetail">
        SELECT ITOP.OPTION_NO AS optionNo
            ,  ITOG.GROUP_NO AS groupNo
            ,  ITOG.GROUP_NAME AS groupName
            ,  ITOP.OPTION_NAME AS optionName
        FROM ITEM_OPTION ITOP
        JOIN ITEM_OPTION_GROUP ITOG ON ITOG.GROUP_NO = ITOP.GROUP_NO
        WHERE ITOP.OPTION_NO IN
        <foreach collection="optionNos" item="no" open="(" separator="," close=")">
            #{no}
        </foreach>
        ORDER BY ITOG.GROUP_NO

    </select>

</mapper>
